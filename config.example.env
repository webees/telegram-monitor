# ===========================================
# Telegram监控系统 - 环境配置文件示例
# 使用说明：
# 1. 复制此文件并重命名为 `.env`
# 2. 填写每个配置项的实际值
# ===========================================


# ===========================================
# Telegram API 配置
# 从 https://my.telegram.org 获取
# ===========================================
TG_API_ID=                 # Telegram 应用的 API ID（整数类型）
TG_API_HASH=               # Telegram 应用的 API HASH（字符串）
# 这两个字段用于连接 Telegram API，是必填项


# ===========================================
# OpenAI API 配置 (用于 AI 监控/智能回复 功能)
# 支持任何符合 OpenAI 接口规范的兼容 API
# ===========================================
OPENAI_API_KEY=            # OpenAI 或兼容服务的 API 密钥
OPENAI_MODEL=gpt-3.5-turbo # 使用的模型名称，例如：gpt-4o, gpt-3.5-turbo 等
OPENAI_BASE_URL=https://api.openai.com/v1 # 自定义 OpenAI 接口地址（如使用代理或第三方服务时填写）


# ===========================================
# 邮件通知配置 (可选)
# 如果启用邮件告警或通知功能，请填写以下信息
# ===========================================
EMAIL_SMTP_SERVER=smtp.gmail.com # SMTP 邮件服务器地址（如 smtp.gmail.com）
EMAIL_SMTP_PORT=587        # SMTP 端口（通常为 465 或 587）
EMAIL_USERNAME=            # 登录 SMTP 服务器的邮箱账号
EMAIL_PASSWORD=            # 邮箱的授权码或密码（注意安全）
EMAIL_FROM=                # 邮件发送者显示的邮箱地址
EMAIL_TO=                  # 邮件接收者（可填多个，用逗号分隔）


# ===========================================
# Web 界面配置
# ===========================================
WEB_HOST=127.0.0.1         # Web 服务监听的主机地址，建议内网使用 127.0.0.1
WEB_PORT=8000              # Web 服务端口号
WEB_DEBUG=false            # 是否开启调试模式（true/false）


# Web 界面认证配置（若系统暴露到公网，请务必启用）
WEB_USERNAME=              # Web 登录用户名
WEB_PASSWORD=              # Web 登录密码


# ===========================================
# 数据存储配置
# ===========================================
DATA_DIR=./data            # 数据存放目录
LOGS_DIR=./logs            # 日志存放目录
DOWNLOADS_DIR=./data/dl    # 下载文件保存目录
